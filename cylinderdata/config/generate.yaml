defaults:
  - _self_
  # - override hydra/launcher: joblib
  - sim: default

count: 4
base_seed: 2710
interval: 2

hydra:
  mode: "MULTIRUN"
  job:
    chdir: true
  sweep:
    dir: ./logs/generate/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: job${hydra.job.num}
#  launcher:
#    n_jobs: 4
